import 'package:flutter/material.dart';

class SaleCommit extends StatefulWidget {
  const SaleCommit({super.key});

  @override
  State<SaleCommit> createState() => _SaleCommitState();
}

class _SaleCommitState extends State<SaleCommit> {
  @override
  Widget build(BuildContext context) {
    return 

    Scaffold(
      appBar: AppBar(title: const Text('Cobrar')),
      body: Center(
        child: Text("h2"),
    ),);
  }
}